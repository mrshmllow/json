[package]
name = "json"
version = "0.1.0"
edition = "2021"

[dependencies]
lalrpop-util = { version = "0.20.2", features = ["lexer"] }

# Add a build-time dependency on the lalrpop library:
[build-dependencies]
lalrpop = "0.20.2"
